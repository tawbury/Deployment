# k8s/lint – 매니페스트·차트 검증

Kubernetes 매니페스트와 Helm 차트의 문법·스키마·정책 검사를 위한 스크립트를 둡니다.

- **내용**: `kubeval`, `kubeconform`, `helm lint`, OPA/Gatekeeper 등
- **실행**: CI 파이프라인 또는 로컬에서 `./tests/k8s/lint/run.sh` 등

검증 스크립트·설정 파일을 이 폴더에 추가하세요.
