# templates/pvc.yaml
# ============================================================
# Persistent Volume Claims
# ============================================================

# Observer Data PVC
{{- if .Values.persistence.data.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.persistence.data.claimName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "observer.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
{{- if .Values.persistence.data.static.enabled }}
  storageClassName: manual
  volumeName: {{ .Values.persistence.data.claimName }}-pv
{{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.data.size }}
{{- end }}
---
# Observer Logs PVC
{{- if .Values.persistence.logs.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.persistence.logs.claimName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "observer.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
{{- if .Values.persistence.logs.static.enabled }}
  storageClassName: manual
  volumeName: {{ .Values.persistence.logs.claimName }}-pv
{{- end }}
  resources:
    requests:
      storage: {{ .Values.persistence.logs.size }}
{{- end }}
---
# PostgreSQL Data PVC
{{- if and .Values.postgres.enabled .Values.postgres.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.postgres.persistence.claimName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "observer.labels" . | nindent 4 }}
    app.kubernetes.io/component: database
spec:
  accessModes:
    - ReadWriteOnce
{{- if .Values.postgres.persistence.static.enabled }}
  storageClassName: manual
  volumeName: {{ .Values.postgres.persistence.claimName }}-pv
{{- end }}
  resources:
    requests:
      storage: {{ .Values.postgres.persistence.size }}
{{- end }}
---
# Observer Backup Archives PVC
{{- if .Values.persistence.backups.archives.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.persistence.backups.archives.claimName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "observer.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: {{ .Values.persistence.backups.archives.claimName }}-pv
  resources:
    requests:
      storage: {{ .Values.persistence.backups.archives.size }}
{{- end }}
---
# Observer Backup Manifests PVC
{{- if .Values.persistence.backups.manifests.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.persistence.backups.manifests.claimName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "observer.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  volumeName: {{ .Values.persistence.backups.manifests.claimName }}-pv
  resources:
    requests:
      storage: {{ .Values.persistence.backups.manifests.size }}
{{- end }}
